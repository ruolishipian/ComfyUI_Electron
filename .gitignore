# Node.js 依赖（仅忽略node_modules，package-lock.json需提交）
node_modules/
# 已删除：package-lock.json  ← 关键修改：移除这行，不再忽略锁文件

# Electron 打包产物（通过 Releases 发布，不提交）
dist/
build/
out/

# 系统/编辑器缓存
.DS_Store
.idea/
.vscode/
*.log
.env

# electron-builder 缓存文件
AppData/